package courier.uy.core.db;

import org.springframework.stereotype.Component;
//import rondanet.catalogo.core.repository.IPalletRepository;

@Component
public class PalletsDAO {

	/*@Autowired
	IPalletRepository palletRepository;

	public Pallet findById(Long id) {
		return palletRepository.findById(id).get();
	}

	public Pallet insert(Pallet toAdd) {
		toAdd.setFechaCreacion();
		toAdd.setFechaEdicion();
		return palletRepository.save(toAdd);
	}

	public void update(Pallet toUpdate) {
		toUpdate.setFechaEdicion();
		palletRepository.save(toUpdate);
	}

	 */

}
